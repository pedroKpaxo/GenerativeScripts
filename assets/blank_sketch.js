// Blank Sketch, the starting point to all the p5.js sketchs.
// Using the globals mode.

function setup() {
  createCanvas(500, 500, WEBGL);
  frameRate(60);
  
}

function draw() {
  background(10);
  
}
